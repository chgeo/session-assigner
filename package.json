{
  "name": "session-assigner",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/sqlite": "^1",
    "axios": "^1.6.2",
    "chai": "^4.3.10",
    "chai-as-promised": "^7.1.1",
    "chai-subset": "^1.6.0"
  },
  "scripts": {
    "build": "npx cds-typer '*' --outputDirectory @cds-models",
    "start": "cds-serve",
    "test": "npx -y jest --silent"
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  }
}
